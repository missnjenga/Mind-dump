# Mind-dump
A react one page app that helps sort out your mind 
/**
 * FloatJournal — an account-free, nothing-saved-to-a-server journal.
 *
 * - Entry text persists only in the browser's localStorage.
 * - Words drift into a slow floating state a couple seconds after you pause typing.
 * - Mic button dictates via the Web Speech API.
 * - "breath: on" listens for someone blowing into the mic (loud + low-frequency)
 *   and, when detected, scatters the letters and clears the entry.
 *
 * Drop this file into a React project (Vite/CRA/Next "use client" component, etc).
 * No extra dependencies required — everything here is plain browser APIs.
 */
